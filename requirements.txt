# reup-video (worker) dependencies
# Root requirements.txt is aligned with worker/pyproject.toml so you can run:
#   pip install -r requirements.txt
#
# ASR (faster-whisper) is optional.
# To install ASR:
#   pip install "faster-whisper>=1.0.0"

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic-settings>=2.6.0
edge-tts>=7.0.0
gTTS>=2.5.0

