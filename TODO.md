# TODO - bổ sung requirements

- [ ] Xác nhận thay thế `requirements.txt` root để đồng bộ dependencies theo `worker/pyproject.toml`.
- [x] Cập nhật `c:/Users/vanha/Downloads/reup/requirements.txt`.

- [ ] (Nếu cần) bổ sung hướng dẫn ASR trong comment.
- [ ] Chạy kiểm tra cài đặt: `pip install -r requirements.txt` (và cài extras ASR nếu dùng real pipeline).
- [ ] Chạy test tối thiểu: `cd worker && pytest`.

